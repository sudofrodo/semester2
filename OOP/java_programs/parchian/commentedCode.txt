
constructors of gameplay:

 // //when default cities are used
    // public GamePlay(int players, ArrayList<String>names)
    // {
    //     Factory.setDefaultCities(players);
    //     playerList = Factory.setPlayersList(names);
    //     this(players);
    // }

    // //when custom cities are made;
    // public GamePlay(int players, ArrayList<String>names, Map<String, Integer> customCities)
    // {
    //     Factory.setCustomCities(customCities);
    //     playerList = Factory.setPlayersList(names);
    //     this(players);   

    // }


    // private void setFinalScore()
    // {
    //        for(Player p: playerList)
    //     {
    //         finalScores.put(p.getName(), 0);
    //     }
    // }

    // private void updateFinalScore()
    // {
    //     for(Player p: winnerList)
    //     {
    //         int currentScore = finalScores.get(p.getName());
    //         int newScore = currentScore + p.getScore();
    //         finalScores.put(p.getName(), newScore);
    //     }
    // }
    
    
    // static
    // {
    // for(Player p: winnerList)
    // {

    // }
    // }

  // updateFinalScore();
     // private Map<String, Integer> finalScores = new HashMap<>();


     // public static ArrayList<Player> sortByHighestScore(Map<String, Integer> playerList) {
    // ArrayList<Player> sortedPlayers = new ArrayList<>(playerList);
    // int n = sortedPlayers.size();
    // for (int j = 0; j < n - 1; j++) { 
    //     for (int i = 0; i < n - j - 1; i++) { 
    //         Player first = sortedPlayers.get(i);
    //         Player second = sortedPlayers.get(i + 1);
    //         if (second.getScore() > first.getScore()) {
    //             Player temp = first;
    //             sortedPlayers.set(i, second);               
    //             sortedPlayers.set(i + 1, temp);
    //         }
    //     }
    // }
    // return sortedPlayers;
    // }
    
    // public static Map<String, Integer> calculateAccumulativeScore(Map<String, Integer> playerScores)
    // {
    //     Map<String, Integer> finalScore = new Map<>();
    //     playerScores.forEach((player, score) -> {
            
    //     });
    // }